# 🎮 Telegram Mini App - Кейс Мастер

Современное Telegram Mini App для открытия кейсов с призами, построенное с использованием лучших практик разработки и развернутое на Railway.

## 🚀 Особенности

- **Современная архитектура**: Модульная структура с ES6+ модулями
- **Безопасность**: Валидация данных Telegram с HMAC-SHA256
- **База данных**: PostgreSQL с Railway
- **Анимации**: Плавные анимации открытия кейсов в стиле CS2
- **Адаптивность**: Работает на всех устройствах
- **Админ панель**: Полнофункциональное управление пользователями и статистика
- **Поиск и пагинация**: Удобный поиск по пользователям с пагинацией

## 🛠 Технологии

- **Frontend**: Vanilla JavaScript (ES6+), CSS3, HTML5
- **Backend**: Node.js, Express.js
- **База данных**: PostgreSQL (Railway)
- **Валидация**: HMAC-SHA256 для безопасности
- **Деплой**: Railway
- **Модули**: ES6 модули для лучшей организации кода

## 📦 Структура проекта

```
├── src/
│   ├── app.js                 # Основной файл приложения
│   ├── config.js             # Конфигурация
│   ├── api.js                # API клиент
│   ├── components/
│   │   ├── UserManager.js    # Управление пользователями
│   │   └── CaseManager.js    # Управление кейсами
│   └── utils/
│       ├── validation.js     # Валидация данных Telegram
│       ├── prizeGenerator.js # Генерация призов
│       └── notifications.js  # Система уведомлений
├── server.js                 # Express сервер
├── database.js              # PostgreSQL подключение
├── index.html               # Главная страница
├── admin.html               # Админ панель
├── styles.css               # Стили
├── compatibility.js         # Совместимость устройств
├── package.json             # Зависимости
├── railway.json             # Конфигурация Railway
└── env.example              # Пример переменных окружения
```

## 🚀 Развертывание на Railway

### 1. Подготовка проекта

1. **Клонируйте репозиторий**
```bash
git clone <your-repo-url>
cd tgMiniAPP-clean
```

2. **Установите зависимости**
```bash
npm install
```

### 2. Настройка Railway

1. **Подключите репозиторий к Railway**
2. **Настройте переменные окружения в Railway:**

#### Обязательные переменные:
- `BOT_TOKEN` - токен вашего Telegram бота
- `DATABASE_URL` - URL вашей PostgreSQL базы данных (автоматически создается Railway)
- `WEBAPP_URL` - URL вашего приложения (например: `https://your-app.railway.app/`)
- `WEBHOOK_URL` - URL для webhook (например: `https://your-app.railway.app/bot/webhook`)

#### Дополнительные переменные:
- `NODE_ENV=production`
- `PORT` - порт (автоматически устанавливается Railway)

### 3. Настройка Telegram бота

1. **Создайте бота через @BotFather**
2. **Получите токен бота**
3. **Настройте webhook:**
```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-app.railway.app/bot/webhook"}'
```

4. **Настройте Mini App:**
```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebApp" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-app.railway.app/"}'
```

## 🔧 Локальная разработка

1. **Создайте .env файл:**
```bash
cp env.example .env
```

2. **Заполните переменные окружения в .env**

3. **Запустите в режиме разработки:**
```bash
npm run dev
```

4. **Запустите продакшн версию:**
```bash
npm start
```

## 🔒 Безопасность

- ✅ Валидация всех данных от Telegram с использованием HMAC-SHA256
- ✅ Проверка подписи init_data на сервере
- ✅ Защита от SQL инъекций через параметризованные запросы
- ✅ Валидация входных данных
- ✅ Админские функции защищены проверкой ID

## 📊 API Endpoints

### Пользовательские
- `POST /api/user/data` - Получение данных пользователя
- `POST /api/user/save` - Сохранение данных пользователя
- `POST /api/case/open` - Открытие кейса
- `POST /api/prize/claim` - Получение приза

### Админские
- `GET /api/admin/users` - Список пользователей
- `GET /api/admin/stats` - Статистика
- `POST /api/admin/users/:id/balance` - Обновление баланса
- `POST /api/admin/users/:id/add-balance` - Добавление к балансу

### Системные
- `POST /bot/webhook` - Webhook для Telegram
- `GET /api/info` - Информация о сервере

## 🎮 Игровая механика

### Типы кейсов
- **Уникальный кейс** (50 ⭐) - Все призы в одном кейсе

### Редкость призов
- **Обычный** (60%) - Базовые призы
- **Редкий** (25%) - Улучшенные призы
- **Эпический** (12%) - Мощные призы
- **Легендарный** (3%) - Уникальные призы

### Типы призов
- **Стикеры** - Коллекционные стикеры
- **Подарки** - Подарки для друзей
- **Звезды** - Внутренняя валюта
- **Premium** - Telegram Premium подписка

## 👥 Админ панель

Доступна по адресу: `https://your-app.railway.app/admin`

### Функции:
- 📊 Просмотр статистики пользователей
- 👥 Список всех пользователей с поиском
- ✏️ Редактирование данных пользователей
- ➕ Добавление звезд пользователям
- 🔄 Автоматическое обновление каждые 30 секунд
- 📱 Адаптивный дизайн

## 🐛 Отладка

### Логи
Все важные события логируются в консоль с эмодзи для удобства:
- 🚀 Инициализация
- ✅ Успешные операции
- ❌ Ошибки
- 🔧 Админские действия
- 📊 Статистика

### Проверка состояния
```bash
curl https://your-app.railway.app/api/info
```

### Мониторинг Railway
- Проверьте логи в Railway Dashboard
- Убедитесь, что все переменные окружения настроены
- Проверьте подключение к базе данных

## 📈 Мониторинг

- Статистика пользователей
- Общий баланс звезд
- Количество открытых кейсов
- Админ панель для управления
- Автоматическое резервное копирование

## 🚀 Развертывание

Railway автоматически:
1. Обнаружит `package.json` и установит зависимости
2. Запустит `npm start`
3. Подключится к PostgreSQL базе данных
4. Создаст необходимые таблицы

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

MIT License - см. файл LICENSE для деталей.

## 📞 Поддержка

Если у вас есть вопросы или проблемы:
1. Проверьте логи в Railway Dashboard
2. Убедитесь, что все переменные окружения настроены
3. Проверьте подключение к базе данных
4. Создайте Issue в репозитории

## 🎯 Готово к использованию!

Ваш Telegram Mini App готов к работе на Railway! Просто настройте переменные окружения и ваш бот будет работать с полной функциональностью.